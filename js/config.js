// Configurazioni generali e costanti
const SPOTIFY_CLIENT_ID = 'your_client_id_here';
const SPOTIFY_REDIRECT_URI = 'your_redirect_uri_here';

const SCOPES = [
  'playlist-read-private',
  'playlist-modify-public',
  'playlist-modify-private',
  'user-library-read'
];

// Config playlist
const DEFAULT_SETTINGS = {
  minDuration: 0,
  maxDuration: 600,
  genres: [],
  languages: [],
  mode: 'auto',
};

// Altri dati statici o impostazioni
